---
title: "Graph Neural Networks for Full Band-Structure Prediction in Architected Metamaterials"
authors:
  - Nicolas Cayuela
  - Mourad Oudich
  - Badreddine Assouar
pub: "Poster"
date: 2026-06-01
cover: assets/images/poster-gnn-band-structure.png
abstract: >-
  <span class="lang-en"><strong class="pub-h">Motivation</strong><br>Designing a metamaterial for a target vibrational response is easier through inverse design. The problem is not bijective: many geometries give the same response, and testing each candidate by simulation is expensive. A design loop therefore needs two networks. A fast forward model that maps shape to response, and a generator that proposes shapes for a chosen response. Most deep learning work reduces the response to a bandgap width and a center frequency. This poster presents the forward model, which predicts the full band structure.<br><strong class="pub-h">Forward model</strong><br>The model treats the unit cell as a graph. Junctions are the nodes, beams are the edges. Node and edge features are projected to dimension 768, then pass through six GINE message-passing layers. Each junction thus aggregates information up to about six nodes away. Ten learned band queries then aggregate the nodes by attention, one per band, and produce one vector per band. A short residual stack of 1D convolutions along the wavenumber axis mixes the bands and smooths the dispersion. The output is ten bands over 311 k-points along Y&Gamma;XM&Gamma;, each with a frequency and a polarization split over three channels (one longitudinal, two transverse).<br>$$h_i^{(l+1)} = \mathrm{ReLU}\!\big(\mathrm{BatchNorm}(\mathrm{GINEConv}(h^{(l)},e))\big) + h_i^{(l)}$$$$\mathrm{GINEConv}(h,e)_i = \mathrm{MLP}_\theta\!\Big((1{+}\epsilon)\,h_i + \!\!\sum_{j\in\mathcal{N}(i)}\!\! \mathrm{ReLU}(h_j + e_{j\to i})\Big)$$$$\alpha_{q,i} = \mathrm{softmax}_i\langle Q_q,\,h_i\rangle,\qquad z_q = \sum_i \alpha_{q,i}\,h_i$$$$\hat{y} = f_\theta(\mathcal{G})\in\mathbb{R}^{4\times 10\times 311}$$<strong class="pub-h">Training</strong><br>Clean training data is the main difficulty: band labeling is not robust across geometries. To get around this, prediction and target are first matched optimally by a Hungarian assignment recomputed at every step, and the error is measured only afterward. The model is scored on the bands it finds, regardless of the order it lists them in. A few auxiliary terms on correlation, slope, and amplitude along k keep the upper bands from flattening. A second head predicts the polarization on the same matching.<br>$$\pi^\star = \arg\min_{\pi\in S_{10}} \sum_{j} \big\lVert \hat{y}_j - y_{\pi(j)}\big\rVert^2$$$$\mathcal{L} = \underbrace{\frac{1}{BK}\sum_{i,k} w_{\pi^\star(i)}\big(\hat{y}_{i,k} - y_{\pi^\star(i),k}\big)^2}_{\text{Magnitude}} + \lambda\,\underbrace{\big(\mathcal{L}_r + \mathcal{L}_{\partial_k} + \mathcal{L}_\sigma\big)}_{\text{Shape}}$$$$\mathcal{L}_r = 1 - \mathrm{corr}_k(\hat{\mathbf{y}},\mathbf{y}),\quad \mathcal{L}_{\partial_k} = \big\lVert \partial_k\hat{\mathbf{y}} - \partial_k\mathbf{y}\big\rVert^2,\quad \mathcal{L}_\sigma = \big(\sigma_k\hat{\mathbf{y}} - \sigma_k\mathbf{y}\big)^2$$<strong class="pub-h">Next</strong><br>The next step is inverse design. A graph VAE will run the reverse path, from a target band structure to a geometry, and the same recipe should transfer to other physical properties.<br><br><strong class="pub-h">Glossary</strong><br>&bull; <strong>GNN / GINE</strong>: graph neural network; GINE is a message-passing layer that accounts for edges.<br>&bull; <strong>Message-passing</strong>: each node updates itself by aggregating information from its neighbors.<br>&bull; <strong>Attention / band query</strong>: learned vectors that weight and aggregate nodes into one embedding per band.<br>&bull; <strong>Residual stack</strong>: layers fed back into their input, refining it instead of replacing it.<br>&bull; <strong>Hungarian matching</strong>: optimal one-to-one matching of predicted and true bands, so the model is scored on the set of curves, not their order.<br>&bull; <strong>Forward / inverse model</strong>: predicts the response from a geometry / generates a geometry for a target response.<br>&bull; <strong>Graph VAE</strong>: generative model on graphs, here for inverse design.<br><br><strong class="pub-h">Notation</strong><br>&bull; \(\hat{y}\): Prediction<br>&bull; \(y\): Target<br>&bull; \(\pi^\star\): Optimal band permutation<br>&bull; \(B\): Batch size<br>&bull; \(K\): k-points per band<br>&bull; \(w_b\): Per-band weight<br>&bull; \(\lambda\): Weight of the shape terms<br>&bull; \(\partial_k\): Derivative along k<br>&bull; \(\sigma_k\): Standard deviation along k<br>&bull; \(\mathrm{corr}_k\): Correlation along k<br>&bull; \(Q_q\): Band query<br>&bull; \(\mathcal{N}(i)\): Neighbours of node \(i\)</span><span class="lang-fr"><strong class="pub-h">Motivation</strong><br>Concevoir un m&eacute;tamat&eacute;riau pour une r&eacute;ponse vibratoire vis&eacute;e est plus simple par inverse design. Le probl&egrave;me n'est pas bijectif&nbsp;: plusieurs g&eacute;om&eacute;tries donnent la m&ecirc;me r&eacute;ponse, et tester chaque candidate par simulation co&ucirc;te cher. Une boucle de conception demande donc deux r&eacute;seaux. Un forward rapide qui va de la forme &agrave; la r&eacute;ponse, et un g&eacute;n&eacute;rateur qui propose des formes pour une r&eacute;ponse choisie. La plupart des travaux en deep learning r&eacute;duisent la r&eacute;ponse &agrave; une largeur de bandgap et une fr&eacute;quence centrale. Ce poster pr&eacute;sente le mod&egrave;le forward, qui pr&eacute;dit la structure de bandes compl&egrave;te.<br><strong class="pub-h">Mod&egrave;le forward</strong><br>Le mod&egrave;le voit la cellule unitaire comme un graphe. Les jonctions sont les nodes, les poutres les edges. Les features de n&oelig;uds et d'ar&ecirc;tes sont projet&eacute;es en dimension 768, puis traversent six couches de message-passing GINE. Chaque jonction agr&egrave;ge ainsi l'information jusqu'&agrave; environ six n&oelig;uds de distance. Dix band queries apprises agr&egrave;gent ensuite les n&oelig;uds par attention, une par bande, et produisent un vecteur par bande. Un court residual stack de convolutions 1D le long de l'axe du nombre d'onde m&eacute;lange les bandes et lisse la dispersion. On obtient dix bandes sur 311 k-points le long de Y&Gamma;XM&Gamma;, chacune avec une fr&eacute;quence et une r&eacute;partition de polarisation en trois canaux (un longitudinal, deux transverses).<br>$$h_i^{(l+1)} = \mathrm{ReLU}\!\big(\mathrm{BatchNorm}(\mathrm{GINEConv}(h^{(l)},e))\big) + h_i^{(l)}$$$$\mathrm{GINEConv}(h,e)_i = \mathrm{MLP}_\theta\!\Big((1{+}\epsilon)\,h_i + \!\!\sum_{j\in\mathcal{N}(i)}\!\! \mathrm{ReLU}(h_j + e_{j\to i})\Big)$$$$\alpha_{q,i} = \mathrm{softmax}_i\langle Q_q,\,h_i\rangle,\qquad z_q = \sum_i \alpha_{q,i}\,h_i$$$$\hat{y} = f_\theta(\mathcal{G})\in\mathbb{R}^{4\times 10\times 311}$$<strong class="pub-h">Entra&icirc;nement</strong><br>Obtenir des donn&eacute;es propres est la principale difficult&eacute;&nbsp;: l'&eacute;tiquetage des bandes n'est pas robuste d'une g&eacute;om&eacute;trie &agrave; l'autre. Pour contourner cela, pr&eacute;diction et cible sont d'abord appari&eacute;es de fa&ccedil;on optimale par une Hungarian assignment recalcul&eacute;e &agrave; chaque pas, et l'erreur n'est mesur&eacute;e qu'ensuite. Le mod&egrave;le est not&eacute; sur les bandes qu'il trouve, quel que soit l'ordre dans lequel il les liste. Quelques termes auxiliaires sur la corr&eacute;lation, la pente et l'amplitude le long de k emp&ecirc;chent les bandes hautes de s'aplatir. Une seconde head pr&eacute;dit la polarisation sur le m&ecirc;me appariement.<br>$$\pi^\star = \arg\min_{\pi\in S_{10}} \sum_{j} \big\lVert \hat{y}_j - y_{\pi(j)}\big\rVert^2$$$$\mathcal{L} = \underbrace{\frac{1}{BK}\sum_{i,k} w_{\pi^\star(i)}\big(\hat{y}_{i,k} - y_{\pi^\star(i),k}\big)^2}_{\text{Magnitude}} + \lambda\,\underbrace{\big(\mathcal{L}_r + \mathcal{L}_{\partial_k} + \mathcal{L}_\sigma\big)}_{\text{Shape}}$$$$\mathcal{L}_r = 1 - \mathrm{corr}_k(\hat{\mathbf{y}},\mathbf{y}),\quad \mathcal{L}_{\partial_k} = \big\lVert \partial_k\hat{\mathbf{y}} - \partial_k\mathbf{y}\big\rVert^2,\quad \mathcal{L}_\sigma = \big(\sigma_k\hat{\mathbf{y}} - \sigma_k\mathbf{y}\big)^2$$<strong class="pub-h">Suite</strong><br>L'&eacute;tape suivante est l'inverse design. Un graph VAE fera le chemin inverse, d'une structure de bandes vis&eacute;e vers une g&eacute;om&eacute;trie, et la m&ecirc;me recette devrait se transposer &agrave; d'autres propri&eacute;t&eacute;s physiques.<br><br><strong class="pub-h">Glossaire</strong><br>&bull; <strong>GNN / GINE</strong>&nbsp;: r&eacute;seau de neurones sur graphe ; GINE est une couche de message-passing tenant compte des edges.<br>&bull; <strong>Message-passing</strong>&nbsp;: chaque n&oelig;ud se met &agrave; jour en agr&eacute;geant l'information de ses voisins.<br>&bull; <strong>Attention / band query</strong>&nbsp;: vecteurs appris qui pond&egrave;rent et agr&egrave;gent les n&oelig;uds en un embedding par bande.<br>&bull; <strong>Residual stack</strong>&nbsp;: couches r&eacute;inject&eacute;es dans leur entr&eacute;e, elles l'affinent au lieu de la remplacer.<br>&bull; <strong>Hungarian matching</strong>&nbsp;: appariement optimal un-&agrave;-un des bandes pr&eacute;dites et vraies, le mod&egrave;le est not&eacute; sur l'ensemble des courbes, pas sur leur ordre.<br>&bull; <strong>Forward / inverse model</strong>&nbsp;: pr&eacute;dit la r&eacute;ponse depuis une g&eacute;om&eacute;trie / g&eacute;n&egrave;re une g&eacute;om&eacute;trie pour une r&eacute;ponse vis&eacute;e.<br>&bull; <strong>Graph VAE</strong>&nbsp;: mod&egrave;le g&eacute;n&eacute;ratif sur graphes, ici pour l'inverse design.<br><br><strong class="pub-h">Notation</strong><br>&bull; \(\hat{y}\)&nbsp;: Pr&eacute;diction<br>&bull; \(y\)&nbsp;: Cible<br>&bull; \(\pi^\star\)&nbsp;: Permutation optimale des bandes<br>&bull; \(B\)&nbsp;: Taille du batch<br>&bull; \(K\)&nbsp;: k-points par bande<br>&bull; \(w_b\)&nbsp;: Poids par bande<br>&bull; \(\lambda\)&nbsp;: Poids des termes de forme<br>&bull; \(\partial_k\)&nbsp;: D&eacute;riv&eacute;e selon k<br>&bull; \(\sigma_k\)&nbsp;: &Eacute;cart-type selon k<br>&bull; \(\mathrm{corr}_k\)&nbsp;: Corr&eacute;lation selon k<br>&bull; \(Q_q\)&nbsp;: Band query<br>&bull; \(\mathcal{N}(i)\)&nbsp;: Voisins du node \(i\)</span>
---
